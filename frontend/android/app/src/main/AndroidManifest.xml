<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Wajib diminta eksplisit di Android 13+ supaya pengingat sewa lokal
         (lihat NotificationScheduler) bisa tampil, diminta lewat dialog
         runtime setelah login, bukan otomatis dari deklarasi ini saja. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Dipakai flutter_local_notifications supaya jadwal pengingat tetap
         akurat sesudah HP di-restart (tanpa ini, jadwal yang belum sempat
         "bunyi" sebelum HP mati bisa hilang begitu saja). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- local_auth (login sidik jari/wajah) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Ambil foto langsung (dokumen verifikasi/QRIS/ulasan) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <application
        android:label="KOSKITA"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link skema kustom "koskita://kos/{id}", langsung
                 berfungsi tanpa perlu domain/verifikasi apapun (tidak
                 seperti App Link https di bawah), dipakai sementara buat
                 tautan dari luar app (mis. dibagikan lewat WhatsApp) sampai
                 App Link produksi siap. Lihat DeepLinkService di Flutter. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="koskita"/>
            </intent-filter>
            <!-- App Link https resmi, android:autoVerify butuh domain
                 GANTI-koskita.example ini diganti domain produksi asli DAN
                 file /.well-known/assetlinks.json di domain itu diisi
                 fingerprint keystore rilis (lihat catatan di routes/web.php
                 backend). Sebelum itu, tautan https ke domain placeholder
                 ini TIDAK akan diverifikasi OS dan cuma buka browser biasa,
                 aman, bukan crash. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="GANTI-koskita.example" android:pathPrefix="/kos"/>
            </intent-filter>
        </activity>
        <!-- Widget layar utama "Kos Terakhir Dilihat", lihat
             KosWidgetProvider.kt & DeepLinkService. -->
        <receiver
            android:name=".KosWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kos_widget_info"/>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Wajib di Android 11+ (package visibility) supaya url_launcher
             bisa menemukan browser buat buka link Kebijakan Privasi/Syarat
             & Ketentuan lewat LaunchMode.externalApplication. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
